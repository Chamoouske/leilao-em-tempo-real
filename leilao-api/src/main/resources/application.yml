spring:
  application:
    name: leilao-api
  profiles:
    active: @spring.profiles.active@

  cloud:
    stream:
      bindings:
        producerLeilao-out-0:
          destination: leiloes

  graphql:
    cors:
      allowed-origins: ${URL_ACCESS}
      allowed-methods: "*"
    graphiql:
      enabled: true
      path: /graphiql
